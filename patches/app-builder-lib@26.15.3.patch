diff --git a/templates/nsis/common.nsh b/templates/nsis/common.nsh
index 5050279..47d938d 100644
--- a/templates/nsis/common.nsh
+++ b/templates/nsis/common.nsh
@@ -45,12 +45,18 @@ Name "${PRODUCT_NAME}" "${DoubleAmpersand}"
 !macroend

 !macro check64BitAndSetRegView
+  !ifdef TALKING_QUILL_FORCE_UNSUPPORTED_ARCH_VALIDATION
+    MessageBox MB_OK|MB_ICONEXCLAMATION "$(x64WinRequired)" /SD IDOK
+    SetErrorLevel 70
+    Quit
+  !endif
   # https://github.com/electron-userland/electron-builder/issues/2420
   ${If} ${IsWin2000}
   ${OrIf} ${IsWinME}
   ${OrIf} ${IsWinXP}
   ${OrIf} ${IsWinVista}
-    MessageBox MB_OK "$(win7Required)"
+    MessageBox MB_OK "$(win7Required)" /SD IDOK
+    SetErrorLevel 70
     Quit
   ${EndIf}

@@ -67,7 +73,8 @@ Name "${PRODUCT_NAME}" "${DoubleAmpersand}"
         SetRegView 64
       ${Else}
         !ifndef APP_32
-          MessageBox MB_OK|MB_ICONEXCLAMATION "$(x64WinRequired)"
+          MessageBox MB_OK|MB_ICONEXCLAMATION "$(x64WinRequired)" /SD IDOK
+          SetErrorLevel 70
           Quit
         !endif
       ${EndIf}
diff --git a/templates/nsis/include/allowOnlyOneInstallerInstance.nsh b/templates/nsis/include/allowOnlyOneInstallerInstance.nsh
index a5cb3fe..25c835e 100644
--- a/templates/nsis/include/allowOnlyOneInstallerInstance.nsh
+++ b/templates/nsis/include/allowOnlyOneInstallerInstance.nsh
@@ -2,9 +2,13 @@
     !include "nsProcess.nsh"
 !endif

-!ifmacrondef customCheckAppRunning
-  !include "getProcessInfo.nsh"
-  Var pid
+!ifmacrondef customUninstallOldVersion
+  !ifmacrondef customUnInstall
+    !ifmacrondef customCheckAppRunning
+      !include "getProcessInfo.nsh"
+      Var pid
+    !endif
+  !endif
 !endif

 # http://nsis.sourceforge.net/Allow_only_one_installer_instance
diff --git a/templates/nsis/include/extractAppPackage.nsh b/templates/nsis/include/extractAppPackage.nsh
index f5470bf..59e67f1 100644
--- a/templates/nsis/include/extractAppPackage.nsh
+++ b/templates/nsis/include/extractAppPackage.nsh
@@ -78,12 +78,27 @@
 !macroend

 !macro decompress
+  !ifdef TALKING_QUILL_FORCE_EXTRACTION_FAILURE_VALIDATION
+    MessageBox MB_OK|MB_ICONEXCLAMATION "$(decompressionFailed)$\nInjected isolated extraction failure." /SD IDOK
+    !ifmacrodef customInstallFailure
+      !insertmacro customInstallFailure
+    !endif
+    SetErrorLevel 70
+    Quit
+  !endif
   !ifdef ZIP_COMPRESSION
+    !define ZIP_DECOMPRESSION_SUCCESS_LABEL zip_decompression_succeeded_${__LINE__}
     nsisunz::Unzip "$PLUGINSDIR\app-$packageArch.zip" "$INSTDIR"
     Pop $R0
-    StrCmp $R0 "success" +3
-      MessageBox MB_OK|MB_ICONEXCLAMATION "$(decompressionFailed)$\n$R0"
+    StrCmp $R0 "success" ${ZIP_DECOMPRESSION_SUCCESS_LABEL}
+      MessageBox MB_OK|MB_ICONEXCLAMATION "$(decompressionFailed)$\n$R0" /SD IDOK
+      !ifmacrodef customInstallFailure
+        !insertmacro customInstallFailure
+      !endif
+      SetErrorLevel 70
       Quit
+    ${ZIP_DECOMPRESSION_SUCCESS_LABEL}:
+    !undef ZIP_DECOMPRESSION_SUCCESS_LABEL
   !else
     !insertmacro extractUsing7za "$PLUGINSDIR\app-$packageArch.7z"
   !endif
@@ -113,7 +128,7 @@
       # Try copying a few times before asking for a user action.
       Goto RetryExtract7za
     ${else}
-      MessageBox MB_RETRYCANCEL|MB_ICONEXCLAMATION "$(appCannotBeClosed)" /SD IDRETRY IDCANCEL AbortExtract7za
+      MessageBox MB_RETRYCANCEL|MB_ICONEXCLAMATION "$(appCannotBeClosed)" /SD IDCANCEL IDCANCEL AbortExtract7za
     ${endIf}

     # As an absolutely last resort after a few automatic attempts and user
@@ -128,6 +143,10 @@
     Goto DoneExtract7za

   AbortExtract7za:
+    !ifmacrodef customInstallFailure
+      !insertmacro customInstallFailure
+    !endif
+    SetErrorLevel 70
     Quit

   RetryExtract7za:
diff --git a/templates/nsis/include/installUtil.nsh b/templates/nsis/include/installUtil.nsh
index 4736774..6f9bdeb 100644
--- a/templates/nsis/include/installUtil.nsh
+++ b/templates/nsis/include/installUtil.nsh
@@ -16,6 +16,7 @@
   CopyFiles /SILENT `${FROM}` `${TO}`
 !macroend

+!ifmacrondef customUninstallOldVersion
 Function GetInQuotes
   Exch $R0
   Push $R1
@@ -80,6 +81,7 @@ Function GetFileParent
     Pop $R1
     Exch $R0
 FunctionEnd
+!endif

 Var /GLOBAL isTryToKeepShortcuts

@@ -101,10 +103,13 @@ Var /GLOBAL isTryToKeepShortcuts
   ${elseif} "${ROOT_KEY}" == "HKEY_LOCAL_MACHINE"
     ReadRegStr "${VAR}" HKEY_LOCAL_MACHINE "${SUB_KEY}" "${NAME}"
   ${else}
-    MessageBox MB_OK "Unsupported ${ROOT_KEY}"
+    MessageBox MB_OK "Unsupported ${ROOT_KEY}" /SD IDOK
+    SetErrorLevel 70
+    Quit
   ${endif}
 !macroend

+!ifmacrondef customUninstallOldVersion
 Function handleUninstallResult
   Var /GLOBAL rootKey_uninstallResult
   Exch $rootKey_uninstallResult
@@ -126,7 +131,7 @@ Function handleUninstallResult
   Return

   ${if} $R0 != 0
-    MessageBox MB_OK|MB_ICONEXCLAMATION "$(uninstallFailed): $R0"
+    MessageBox MB_OK|MB_ICONEXCLAMATION "$(uninstallFailed): $R0" /SD IDOK
     DetailPrint `Uninstall was not successful. Uninstaller error code: $R0.`
     SetErrorLevel 2
     Quit
@@ -246,3 +251,4 @@ FunctionEnd
   Push "${ROOT_KEY}"
   Call uninstallOldVersion
 !macroend
+!endif
diff --git a/templates/nsis/include/installer.nsh b/templates/nsis/include/installer.nsh
index 300ef3d..b72ee8f 100644
--- a/templates/nsis/include/installer.nsh
+++ b/templates/nsis/include/installer.nsh
@@ -64,7 +64,7 @@
           ${if} $3 == $1
             Goto fun_extract
           ${else}
-            MessageBox MB_OK "Package file $4 found locally, but checksum doesn't match — expected $1, actual $3.$\r$\nLocal file is ignored and package will be downloaded from Internet."
+            MessageBox MB_OK "Package file $4 found locally, but checksum doesn't match — expected $1, actual $3.$\r$\nLocal file is ignored and package will be downloaded from Internet." /SD IDOK
           ${endIf}
         ${endIf}
       ${endIf}
diff --git a/templates/nsis/installSection.nsh b/templates/nsis/installSection.nsh
index 053772f..37edfad 100644
--- a/templates/nsis/installSection.nsh
+++ b/templates/nsis/installSection.nsh
@@ -11,30 +11,32 @@ StrCpy $appExe "$INSTDIR\${APP_EXECUTABLE_FILENAME}"
 # must be called before uninstallOldVersion
 !insertmacro setLinkVars

-!ifdef ONE_CLICK
-  !ifdef HEADER_ICO
-    File /oname=$PLUGINSDIR\installerHeaderico.ico "${HEADER_ICO}"
-  !endif
-  ${IfNot} ${Silent}
+!ifmacrondef customUninstallOldVersion
+  !ifdef ONE_CLICK
     !ifdef HEADER_ICO
-      SpiderBanner::Show /MODERN /ICON "$PLUGINSDIR\installerHeaderico.ico"
-    !else
-      SpiderBanner::Show /MODERN
+      File /oname=$PLUGINSDIR\installerHeaderico.ico "${HEADER_ICO}"
     !endif
-
-    FindWindow $0 "#32770" "" $hwndparent
-    FindWindow $0 "#32770" "" $hwndparent $0
-    GetDlgItem $0 $0 1000
-    SendMessage $0 ${WM_SETTEXT} 0 "STR:$(installing)"
-
-    StrCpy $1 $hwndparent
-		System::Call 'user32::ShutdownBlockReasonCreate(${SYSTYPE_PTR}r1, w "$(installing)")'
-  ${endif}
-  !insertmacro CHECK_APP_RUNNING
-!else
-  ${ifNot} ${UAC_IsInnerInstance}
+    ${IfNot} ${Silent}
+      !ifdef HEADER_ICO
+        SpiderBanner::Show /MODERN /ICON "$PLUGINSDIR\installerHeaderico.ico"
+      !else
+        SpiderBanner::Show /MODERN
+      !endif
+
+      FindWindow $0 "#32770" "" $hwndparent
+      FindWindow $0 "#32770" "" $hwndparent $0
+      GetDlgItem $0 $0 1000
+      SendMessage $0 ${WM_SETTEXT} 0 "STR:$(installing)"
+
+      StrCpy $1 $hwndparent
+      System::Call 'user32::ShutdownBlockReasonCreate(${SYSTYPE_PTR}r1, w "$(installing)")'
+    ${endif}
     !insertmacro CHECK_APP_RUNNING
-  ${endif}
+  !else
+    ${ifNot} ${UAC_IsInnerInstance}
+      !insertmacro CHECK_APP_RUNNING
+    ${endif}
+  !endif
 !endif

 Var /GLOBAL keepShortcuts
@@ -49,13 +51,17 @@ ${if} $isTryToKeepShortcuts == "true"
   ${endIf}
 ${endif}

-!insertmacro uninstallOldVersion SHELL_CONTEXT
-!insertmacro handleUninstallResult SHELL_CONTEXT
+!ifmacrodef customUninstallOldVersion
+  !insertmacro customUninstallOldVersion
+!else
+  !insertmacro uninstallOldVersion SHELL_CONTEXT
+  !insertmacro handleUninstallResult SHELL_CONTEXT

-${if} $installMode == "all"
-  !insertmacro uninstallOldVersion HKEY_CURRENT_USER
-  !insertmacro handleUninstallResult HKEY_CURRENT_USER
-${endIf}
+  ${if} $installMode == "all"
+    !insertmacro uninstallOldVersion HKEY_CURRENT_USER
+    !insertmacro handleUninstallResult HKEY_CURRENT_USER
+  ${endIf}
+!endif

 SetOutPath $INSTDIR

@@ -64,6 +70,11 @@ SetOutPath $INSTDIR
 !endif

 !insertmacro installApplicationFiles
+
+!ifmacrodef customInstallCommit
+  !insertmacro customInstallCommit
+!endif
+
 !insertmacro registryAddInstallInfo
 !insertmacro addStartMenuLink $keepShortcuts
 !insertmacro addDesktopLink $keepShortcuts
diff --git a/templates/nsis/installer.nsi b/templates/nsis/installer.nsi
index 4b1f42e..c1f5a6a 100644
--- a/templates/nsis/installer.nsi
+++ b/templates/nsis/installer.nsi
@@ -47,6 +47,10 @@ Var oldMenuDirectory
 !endif

 Function .onInit
+  !ifmacrodef customEarlyInit
+    !insertmacro customEarlyInit
+  !endif
+
   Call setInstallSectionSpaceRequired

   SetOutPath $INSTDIR
@@ -109,7 +113,8 @@ Section "install" INSTALL_SECTION_ID
                 ${Case} 1223 ;user aborted
                   ${Break}
                 ${Default}
-                  MessageBox mb_IconStop|mb_TopMost|mb_SetForeground "Unable to elevate, error $0"
+                  MessageBox mb_IconStop|mb_TopMost|mb_SetForeground "Unable to elevate, error $0" /SD IDOK
+                  SetErrorLevel 70
                   ${Break}
               ${EndSwitch}
               Quit
diff --git a/templates/nsis/multiUserUi.nsh b/templates/nsis/multiUserUi.nsh
index 52a83df..260c4e7 100644
--- a/templates/nsis/multiUserUi.nsh
+++ b/templates/nsis/multiUserUi.nsh
@@ -160,7 +160,7 @@ Var isForceCurrentInstall
             ${EndIf}
             ${If} $1 = 3 ;RunAs completed successfully, but with a non-admin user
             ${OrIf} $2 = 0x666666 ;our special return, the new process was not admin after all
-              MessageBox mb_IconStop|mb_TopMost|mb_SetForeground "$(loginWithAdminAccount)"
+              MessageBox mb_IconStop|mb_TopMost|mb_SetForeground "$(loginWithAdminAccount)" /SD IDOK
             ${EndIf}
             ${Break}
           ${Case} 1223 ;user aborted
@@ -168,11 +168,11 @@ Var isForceCurrentInstall
             ;Quit ; instead of quit just abort going to the next page, and stay in the radiobuttons
             ${Break}
           ${Case} 1062
-            MessageBox mb_IconStop|mb_TopMost|mb_SetForeground "Logon service not running, aborting!" ; "Unable to elevate, Secondary Logon service not running!"
+            MessageBox mb_IconStop|mb_TopMost|mb_SetForeground "Logon service not running, aborting!" /SD IDOK ; "Unable to elevate, Secondary Logon service not running!"
             ;Quit ; instead of quit just abort going to the next page, and stay in the radiobuttons
             ${Break}
           ${Default}
-            MessageBox mb_IconStop|mb_TopMost|mb_SetForeground "Unable to elevate, error $0"
+            MessageBox mb_IconStop|mb_TopMost|mb_SetForeground "Unable to elevate, error $0" /SD IDOK
             ;Quit ; instead of quit just abort going to the next page, and stay in the radiobuttons
             ${Break}
         ${EndSwitch}
diff --git a/templates/nsis/uninstaller.nsh b/templates/nsis/uninstaller.nsh
index c3f4f11..61e2682 100644
--- a/templates/nsis/uninstaller.nsh
+++ b/templates/nsis/uninstaller.nsh
@@ -1,8 +1,14 @@
 Function un.checkAppRunning
-  !insertmacro CHECK_APP_RUNNING
+  !ifmacrondef customUnInstall
+    !insertmacro CHECK_APP_RUNNING
+  !endif
 FunctionEnd

 Function un.onInit
+  !ifmacrodef customUnEarlyInit
+    !insertmacro customUnEarlyInit
+  !endif
+
   SetOutPath $INSTDIR
   ${LogSet} on

@@ -19,7 +25,8 @@ Function un.onInit
     call un.checkAppRunning
   ${else}
     !ifdef ONE_CLICK
-      MessageBox MB_OKCANCEL "$(areYouSureToUninstall)" IDOK +2
+      MessageBox MB_OKCANCEL "$(areYouSureToUninstall)" /SD IDCANCEL IDOK +3
+      SetErrorLevel 70
       Quit

       # one-click installer executes uninstall section in the silent mode, but we must show message dialog if silent mode was not explicitly set by user (using /S flag)
