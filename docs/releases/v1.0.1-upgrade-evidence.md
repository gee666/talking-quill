# v1.0.0 → v1.0.1 isolated NSIS lifecycle evidence

- Historical source: `0e82f3ce30906851679924a29a736eae386fb225` (detached worktree under ignored `tmp/`)
- Historical rebuilt installer: `Talking-Quill-1.0.0-win-x64.exe`
- Historical rebuild size: `114653899` bytes
- Historical rebuild SHA-256: `7dd70b133842570bab02d08d13e0f8b835a642ca7006eabfa55c641536149b8e`
- Harness: `scripts/nsis-upgrade-evidence.mjs`
- Isolation: install directory, `APPDATA`, `LOCALAPPDATA`, `USERPROFILE`, markers, and unrelated-data sentinel are all rooted below ignored `tmp/nsis-lifecycle/`.

The executable harness installs the historical rebuild, seeds settings/history/model markers, installs the current v1.0.1 candidate over the same isolated directory, and requires every marker hash to remain unchanged. It then proves that default silent uninstall preserves those marker hashes. A second installation invokes the same challenge-bound reset helper used by the destructive uninstall option, then uninstalls; it requires the isolated Talking Quill data root to be removed and an external sentinel to remain unchanged. It also requires the install tree to disappear after both uninstalls.

This evidence exercises only disposable paths and does not use the operator's Talking Quill profile or normal installation directory. The current candidate hash is intentionally taken from the final current-HEAD build and reported with the release artifact; rebuilding changes unsigned NSIS bytes.
