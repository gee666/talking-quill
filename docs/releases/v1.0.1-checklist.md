# v1.0.1 release checklist

## Completed engineering evidence

- [x] Frozen install, format, lint, typecheck, Knip, full tests, and coverage pass.
- [x] Source Chromium and the separately named `Talking Quill Packaged Test` instrumented composition drive fake media through production capture/worklet/session/widget code and verify level, accessible state, reduced motion, cancel, reload, and reset. This is packaged-test evidence, not evidence that the production installer exposes an activation driver.
- [x] Rust format, Clippy, helper tests, and Pi bootstrap tests pass.
- [x] Security, dependency, secret-history, and network-boundary gates pass.
- [x] Authentic npm-global Pi 0.81.1 proves list, exact model, thinking, print completion, cancellation, descendant termination, JS races, and native-materialization tamper rejection without a billable provider call.
- [x] Clean Windows x64 NSIS packaging produces exactly `Talking-Quill-1.0.1-win-x64.exe`.
- [x] The separately rebuilt production package excludes the packaged-test composition; recursive package inspection, RunAsNode fuse checks, packaged smoke, and provenance checks pass.
- [x] The isolated historical-source v1.0.0 rebuild upgrade/default-uninstall/destructive-uninstall matrix preserves or removes only the expected disposable markers; see [upgrade evidence](v1.0.1-upgrade-evidence.md).
- [x] The final current-HEAD artifact checksum and source revision are recorded with the candidate results.

## Publication controls

Signing and external release-control approval are deliberately outside this local engineering run. They must be supplied before publication; no GitHub release was created or claimed.
