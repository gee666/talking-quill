export default {
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
  printWidth: 100,
  tabWidth: 2,
  // Accept the checkout's native line endings on Windows and macOS without rewriting files.
  endOfLine: 'auto',
};
